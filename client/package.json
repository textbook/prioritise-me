{
  "name": "@prioritise/client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8"
  },
  "devDependencies": {
    "jest-junit": "^6.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --reporters=\"default\"",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "../package.json",
      "react-app"
    ],
    "settings": {
      "react": {
        "version": "^16.8.6"
      }
    }
  },
  "jest-junit": {
    "outputDirectory": "../output/jest"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
